<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/photoBackgroundColor"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/dialog_photo_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:adjustViewBounds="true" />

    <ImageButton
        android:id="@+id/dialog_photo_delete"
        style="@style/photoButton"
        android:layout_alignParentRight="true"
        android:src="@mipmap/ic_delete" />

    <ImageButton
        android:id="@+id/dialog_photo_return"
        style="@style/photoButton"
        android:layout_alignParentLeft="true"
        android:src="@mipmap/ic_return" />

    <TextView
        android:id="@+id/dialog_photo_date"

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/all_spacing_small"

        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"

        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/text_size_largeish" />
</RelativeLayout>